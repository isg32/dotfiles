# hyprpaper starts with no wallpaper preloaded — conf/autostart.conf calls
# scripts/wallpaper-restore.sh right after it starts, which preloads and sets
# either the last-used wallpaper (~/.config/hypr/current_wallpaper) or a random
# one from ~/Pictures/wallpapers on first run.
splash = false
ipc = on
