{
    "layer": "top",
    "position": "top",
    "height": 34,
    "spacing": 2,
    "margin-top": 6,
    "margin-left": 10,
    "margin-right": 10,

    "modules-left": ["hyprland/workspaces", "hyprland/window"],
    "modules-center": ["clock"],
    "modules-right": [
        "tray",
        "custom/wallpaper",
        "custom/power-profile",
        "custom/gpu",
        "pulseaudio",
        "network",
        "bluetooth",
        "battery",
        "custom/notification"
    ],

    "hyprland/workspaces": {
        "format": "{id}",
        "on-click": "activate",
        "persistent-workspaces": { "*": 5 }
    },

    "hyprland/window": {
        "format": "{title}",
        "max-length": 45,
        "separate-outputs": true
    },

    "clock": {
        "format": "{:%H:%M  %a %d %b}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt>{calendar}</tt>",
        "on-click": "gnome-calendar"
    },

    "tray": {
        "icon-size": 16,
        "spacing": 10
    },

    // Opens GNOME's own Sound settings panel; scroll to change volume instantly.
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "󰝟 muted",
        "format-icons": {
            "default": ["󰕿", "󰖀", "󰕾"]
        },
        "scroll-step": 5,
        "on-click": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
        "on-click-right": "env XDG_CURRENT_DESKTOP=GNOME gnome-control-center sound",
        "on-scroll-up": "wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+",
        "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-",
        "tooltip-format": "{desc}\nClick: mute  Right-click: Sound settings"
    },

    // Opens GNOME's own Wi-Fi settings panel on click.
    "network": {
        "format-wifi": "󰤨 {essid}",
        "format-ethernet": "󰈀 wired",
        "format-disconnected": "󰤭 offline",
        "tooltip-format-wifi": "{essid} ({signalStrength}%)\nClick: Wi-Fi settings",
        "on-click": "env XDG_CURRENT_DESKTOP=GNOME gnome-control-center wifi"
    },

    // Opens GNOME's own Bluetooth settings panel on click.
    "bluetooth": {
        "format": "󰂯 {status}",
        "format-connected": "󰂱 {device_alias}",
        "format-off": "󰂲 off",
        "tooltip-format": "Click: Bluetooth settings",
        "on-click": "env XDG_CURRENT_DESKTOP=GNOME gnome-control-center bluetooth"
    },

    // Opens GNOME's own Power settings panel on click.
    "battery": {
        "format": "{icon} {capacity}%",
        "format-charging": "󰂄 {capacity}%",
        "format-icons": ["󰁺", "󰁼", "󰁾", "󰂀", "󰂂", "󰁹"],
        "states": { "warning": 20, "critical": 10 },
        "on-click": "env XDG_CURRENT_DESKTOP=GNOME gnome-control-center power",
        "tooltip-format": "{timeTo}\nClick: Power settings"
    },

    "custom/wallpaper": {
        "format": "󰸉",
        "tooltip": "Left-click: pick wallpaper  ·  Right-click: next wallpaper",
        "on-click": "~/.config/hypr/scripts/wallpaper-picker.sh",
        "on-click-right": "~/.config/hypr/scripts/wallpaper-next.sh"
    },

    // Cycles quiet/balanced/performance (power-profiles-daemon). Same action as Fn+Q.
    "custom/power-profile": {
        "exec": "~/.config/hypr/scripts/power-profile-status.sh",
        "on-click": "~/.config/hypr/scripts/power-profile-cycle.sh",
        "return-type": "json",
        "interval": 3
    },

    // Shows which GPU this session is primary on. Only meaningful on a hybrid
    // Intel+NVIDIA machine — remove this module from modules-right above if
    // you don't have one. Same action as Fn+G.
    "custom/gpu": {
        "exec": "~/.config/hypr/scripts/gpu-status.sh",
        "on-click": "~/.config/hypr/scripts/gpu-switch.sh",
        "return-type": "json",
        "interval": "once"
    },

    "custom/notification": {
        "format": "󰂚",
        "tooltip": "Notifications / quick toggles",
        "on-click": "swaync-client -t -sw"
    }
}
